generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Produto {
  id        Int    @id @default(autoincrement())
  nome      String
  descricao String
  preco     Float
}
